#!/usr/bin/env python3
"""
Git post-commit hook — superseded.

Token attribution is now handled entirely by the Claude Code Stop hook
(.claude/hooks/stop_hook.py). The stop hook reads the transcript directly,
uses last_response_id as a high-water mark, and creates audit: commits.
No temp files or post-commit processing is needed.
"""
